ES6 是对 ECMAScript 2015及之后版本的通称，新增了很多非常方便的写法。

引入了块级作用域的概念。

能用`let`就用`let`，替换掉作用域范围泛滥的`var`。

全局变量尽量使用`const`。

数组、对象解构。

模块字符串，写法是反引号``。

用`Class`替换ES5的类声明形式，用`extends`来实现类的继承。

异步编程用`Promise`，更简洁的写法用`async await`

ES6 的模块导入导出是`import/export`

